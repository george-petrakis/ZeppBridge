# ZeppBridge 视觉与设备 UI 验收清单

> 更新：2026-08-15（重构全量通过）。P1~P7 阶段目标全部达成并验证。

## 全局视觉与交互 (P1)

- [x] 深色基础 token 使用 `#12140D` / `#14160C` / `#1B1E12` / `#22261A` / `#F3F4EC` / `#CDDC7C`。
- [x] 心率（`#F0616A`）、配速/距离（`#4AA8E8`）、消耗（`#F5860B`）、海拔（`#F5C33B`）、步频（`#4AA8E8`）、训练/就绪（`#3DD84C`）及睡眠阶段（深紫 `#7B4FB3`、浅睡 `#7C8FF0`、REM `#2FA96B`、清醒 `#E84C3D`）使用统一语义色。
- [x] 全局细滚动条（4px）、卡片不依赖彩色外发光。
- [x] 页面使用 CSS Grid 与响应式断点；动画仅使用 `transform` / `opacity`，并提供 `prefers-reduced-motion` 降级。
- [x] 品牌名称全局修正为 `ZeppBridge`，云端服务统一命名为 `Zepp Cloud`。

## 探索页 (P2)

- [x] Hero 区域展示三大核心价值卡与数据流转链条。
- [x] 实体设备与 Zepp Cloud 云状态卡展示清晰。
- [x] 最新数据包指标卡与 AI 就绪度环（CircularProgress）展示 4 项数据流就绪状态。
- [x] 底部展示 4 项数据安全保证横条。

## 导出与提示词页 (P3)

- [x] 自绘深橄榄底日历弹层选择器，支持 7 天 / 30 天快捷切换与跨月份点选。
- [x] 移除冗余代码预览大块，保留精简数据包摘要。
- [x] AI 提示词复制与 7 个主流 AI 平台一键跳转联动正常。
- [x] 数据包体积 > 2 MiB 时直接由后端安全写入系统桌面 `zeppbridge-ai-handoff.json` 并向剪贴板写入拖入提示。

## 设置页 (P4)

- [x] 2 列 + 3 列卡片网格布局，信息结构清晰。
- [x] 默认导出格式与本地 localStorage 持久化绑定。
- [x] 移除无后端支持的“管理数据来源”与死开关，隐私政策弹窗就地展开。
- [x] 包含 `#account-section` 与 `#privacy-section` 锚点导航支持。

## 历史记录与详情页 (P5)

- [x] 最近记录页统一两列行高与“共 N 条”计数口径；隐藏不完整数据时显示明确透明提示。
- [x] 运动详情图表采用垂直渐变填充 + 虚线均值线，本地 GPS 画布按速度色谱着色。
- [x] 睡眠详情采用新阶段色，新增近 7 天每日睡眠时长分期堆叠周柱状图（ECharts）。

## 数据层与容错 (P6)

- [x] 运动明细拉取支持所有具备 `zepp_source` 的户外运动（骑行、跑步、徒步等）。
- [x] 增量同步窗口扩展为 30 天。
- [x] 单项明细拉取失败记录日志并不阻断整体同步流程。

## 自动化门禁 (P7)

- [x] `npm run build`（vue-tsc + Vite）编译通过，0 错误。
- [x] `cargo check` 后端检查通过，0 错误。
- [x] `cargo test` 62 个单测 100% 全部通过（62 passed; 0 failed）。
