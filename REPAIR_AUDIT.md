# ZeppBridge 修复审计与剩余项

本文记录 v0.2.x 的缺陷处置状态。可复现门禁和安装验收范围见 [COMPLETION_REPORT.md](COMPLETION_REPORT.md)。

## 原反馈闭环

| 原问题 | 当前处置 | 状态 |
| --- | --- | --- |
| 同步后心率仍旧，拉取时间被当成数据时间 | 云端同步时间与每个流的最新样本时间分开；同步前后比较样本推进 | 已修复并用真实数据验证 |
| 同步成功后界面仍显示未同步 | 单例同步控制器统一状态、自动刷新当前页；启动恢复读取持久化结果 | 已修复 |
| 已保存认证反复要求验证 | 已保存认证启动即为 `connected`；只有 401/403 或 `needs_reauth` 重新连接 | 已修复 |
| 连接与数据页拥挤、表格混乱 | 重命名为“设置”，常用项前置，教程/诊断/导出/维护折叠，逐流状态改纵向行 | 已修复 |
| 概览等权卡片过多、时间语义混乱 | 重排为主信息、数据新鲜度、指标分组和最近记录；移除常驻本地刷新 | 已修复 |
| 睡眠、运动不能进入详情 | 增加详情 IPC、路由、可点击列表和真实字段页面 | 已修复 |
| 运动缺距离、热量、平均/最高心率等 | 详情显示现有真实字段，并补训练负荷、VO₂max 和有效条件下的配速 | 已修复 |
| “已保存”黄色歧义、主题原生下拉难看 | 绿色已连接、灰色中性、黄色关注、红色失败；自定义三态主题菜单 | 已修复 |
| “云端暂无新数据”仍是黄色 | 调整为灰色中性；部分失败才使用黄色 | 本轮补修 |
| 清除/更换认证后残留旧同步结果 | `refreshStatus` 按新连接状态重建 UI，运行中同步保留本次精确文案 | 本轮补修 |

## 当前已验证

- 前端 build、Rust fmt/check/clippy、61 项单测和 `build.bat` 全部通过。
- 新生成 NSIS 覆盖安装退出码 0；实际安装路径、版本、hash 与签名状态已记录。
- 安装版重启后无需再次验证，真实启动同步成功并推进最新心率样本。
- 安装版概览把 10:04 云端同步与 00:16 心率样本分开显示。
- 真实睡眠与运动字段与 SQLite 一致；不存在的路线/采样不显示虚构内容。
- 1440px 与 760px 浏览器视觉烟测无横向溢出、无 console/page error。

## 仍待后续或需要外部条件

### 产品/兼容性

1. **实体手机首次连接**：需在用户指定的 Android 设备上验证 CA 安装、Wi‑Fi 代理、Zepp 请求和收尾恢复。证书固定或系统策略失败时不绕过安全机制。
2. **多区域、多设备、多账号**：当前证据不能外推到所有 Zepp host、设备和固件。
3. **分页与私有 API 演进**：已有窗口限制、重试和结构校验；若真实 endpoint 返回 cursor，需要依据真实响应补分页，不猜测字段。
4. **屏幕阅读器与对比度**：键盘、焦点、ARIA、reduced-motion 和 760px 已实现；完整 NVDA/高对比度人工验收仍待执行。

### 数据边界

1. 缺少已验证的 `workout_samples`、`route_points` 时，不显示采样曲线或地图。
2. 数据来源继续保留 `user_fused / device / unknown`，不新增自动融合或估算。
3. retention 可由用户在 1–365 天内选择（默认 365 天）；SQLite 仍为明文，整库加密属于发布工程，不能在没有迁移方案时临时加入。

### 公开发布工程

1. NSIS、MSI 和 EXE 均为 `NotSigned`；需正式代码签名证书。
2. 自动更新、SBOM、干净 Windows VM、卸载/升级矩阵和发布审计未完成。
3. REST API、MCP 和更多数据源仍是后续阶段，不是本轮遗留 bug。

## 完成判断

用户最初列出的同步、认证、设置、概览、详情、运动字段和颜色问题已闭环。上面的剩余项均需要特定手机/账号/区域、正式发布凭据，或属于明确后续产品阶段；不应用模拟数据或未经验证的私有 API 假设来“伪完成”。
