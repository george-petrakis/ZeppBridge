# ZeppBridge 前端视觉、缩放与图标一致性改造需求

我正在继续开发 ZeppBridge。现在项目的核心功能已经基本可以工作，但前端视觉、页面比例、界面缩放、图标体系以及部分详情页的数据语义，与我提供的设计参考仍然存在明显差距。

本次修改请先进入 Plan Mode 审视现有实现，再决定具体改法。本说明描述的是最终需求、设计约束和验收目标，不要求按照某种固定技术方案实施。

## 一、设计参考的优先级

我提供的图片分为两组：

1. C:\Users\15pro\Desktop\MyProject\ZeppBridge\design_picture里的是目标参考图。
2. C:\Users\15pro\Pictures\Screenshots是当前 ZeppBridge 实际运行效果。

请把前五张参考图视为本轮视觉设计的主要 source of truth。

现有版本可以保留业务逻辑、真实数据逻辑、本地优先原则和已有功能，但视觉层级、尺寸关系、密度、颜色、图标、页面布局应主动向参考图靠拢。

不要继续根据当前 UI 自我延伸设计，也不要仅做局部润色。当前 UI 本身已经偏离目标，如果继续沿用现有比例和组件风格，最终只会得到“更整齐的当前版本”，依旧达不到参考图的效果。

## 二、当前存在的核心 UI 问题

### 1. 页面比例和内容密度明显不正确

当前界面在较大窗口下显得过小、过空，内容集中在中间区域，参考图则拥有更自然、更饱满的桌面应用比例。

代码中目前存在多组独立的页面宽度约束：

`App.vue` 的通用 `.page` 最大宽度为约 1120px。

`Settings.vue` 又单独限制在约 920px。

睡眠详情和运动详情限制在约 980px。 

AI 页面又单独使用约 1180px。

这导致不同页面之间的视觉宽度、信息密度和留白比例都不一致。

Tauri 配置当前还设置默认窗口为 1280 × 800，并且 Rust 启动代码会根据显示器高度重新设置窗口，同时保持宽度为 1280。 

请重新建立统一的桌面布局尺寸体系，使界面在普通笔记本、大尺寸显示器和 Windows 缩放环境下都保持与参考图接近的比例。

目标效果：

内容区应随着窗口扩大合理扩展。

不要在宽屏下留下大面积无意义的左右空白。

侧边栏、顶部栏、主卡片、次级卡片、字体和间距应形成统一比例。

各页面不能分别拥有明显不同的视觉缩放感。

1280 宽窗口仍应正常使用。

更大的窗口应真正利用额外空间。

## 三、增加真正可用的界面缩放功能

目前用户基本只能改变窗口大小，无法主动控制 UI 的视觉缩放。

本次希望增加一套明确的界面缩放能力。

至少应允许用户选择多档比例，例如：

80%

90%

100%

110%

125%

具体档位可以根据实际体验微调。

要求：

缩放设置需要持久化。

重新打开应用后继续使用上次比例。

缩放必须作用于整个 UI 系统，包括文字、卡片、侧栏、按钮、图表和间距。

不能只改变字体。

可以考虑支持 Ctrl 加号、Ctrl 减号和 Ctrl 0 作为辅助操作。

100% 应作为设计基准，视觉结果应首先对齐参考图。

缩放不能破坏响应式布局。

设置页面中需要有自然、简洁的入口。

## 四、建立统一的视觉设计系统

当前各页面存在比较明显的独立设计痕迹。

例如全局已经定义了一套颜色变量，但 AI 页面又重新维护了一整套 `ai-accent`、`ai-shell`、`ai-line` 等局部设计变量。Overview 中也存在多处直接硬编码颜色。  

请把整个应用重新统一到一套明确的设计语言。

希望保留参考图中的这些感觉：

深色背景有层次，但不要纯黑。

卡片之间通过极轻的色差和描边区分。

绿色为主品牌色。

心率使用红色系。

睡眠使用蓝紫色系。

运动使用绿色系。

状态色保持克制。

圆角明显但不要过度圆润。

边框要细。

阴影非常克制。

不要添加强烈玻璃拟态。

不要使用大面积发光效果。

不要产生廉价的游戏面板感。

文字层级应该明显。

标题、主要数字、辅助信息、注释之间应有清楚的字号和颜色区别。

参考图整体更像成熟的健康数据桌面产品，希望修改后的 ZeppBridge 达到同一类完成度。

## 五、概览页要求

概览页是整个项目最重要的视觉页面，请优先保证它与参考图接近。

心率主卡和步数卡的面积比例、纵向高度和整体存在感需要参考第一张图重新校准。

心率卡应该拥有足够的宽度，使曲线具有阅读价值。

步数圆环不能显得过小。

下面三个指标卡需要保持紧凑。

睡眠、静息心率、最近运动三张卡的高度应接近参考图。

“交给 AI”入口应作为一条横向强调卡存在，但不能抢过核心健康数据。

底部的最近睡眠、最近运动列表应保持紧凑。

整个概览尽量让用户在一个正常桌面窗口内看到主要内容。

不要因为统一使用很大的卡片和间距，导致用户需要进行大量垂直滚动。

## 六、交给 AI 页面要求

该页面整体结构方向是正确的，但当前版本的尺寸、留白和信息密度距离参考图较远。

目标参考第二张图。

左侧保持：

时间范围。

数据类型。

导出操作。

导出结果。

右侧保持：

导出摘要。

JSON 预览。

需要让左右区域在桌面窗口中形成明确的主次关系。

按钮、输入框、Chip、JSON 面板的尺寸应进一步向参考图靠拢。

不要让页面标题和卡片显得过大。

尽量使最常用的操作在一个屏幕内完成。

保持当前“不在应用内部分析用户数据，只负责输出标准化 JSON”的产品定位。

## 七、设置页面要求

设置页当前的信息组织方向基本合理，但视觉尺寸偏大，纵向占用过多。

目标参考第三张图。

希望恢复这种布局关系：

顶部为 Zepp 云端连接卡。

中间第一行左侧自动同步，右侧保留天数。

第二行左侧历史补拉，右侧外观主题。

下面是高级与隐私。

最后是重新连接 Zepp。

不要让设置页因为字号、padding、卡片高度过大而产生明显的“网页后台”感觉。

高级与隐私可以保持折叠能力。

展开后的工具列表需要继续保持参考图中的紧凑风格。

危险操作仍然应该保留明确的红色视觉提示。

## 八、二级页面进入时必须回到顶部

当前 Router 配置没有定义滚动行为。

从一个已经滚动较深的页面进入睡眠详情、运动详情或其他页面时，应确保新页面默认回到顶部。

不能出现当前截图中类似“进入新页面后左侧 Logo 和页面顶部已经被滚出屏幕”的体验。

返回上一页时，可以根据交互合理决定恢复原位置或回到顶部，但进入一个新的详情页时必须从页面顶部开始。

## 九、睡眠详情页面要求

视觉目标参考第四张图。

保留：

睡眠时长主卡。

睡眠评分。

睡眠阶段。

深睡、浅睡、REM、清醒统计。

来源。

设备信息。

希望增加参考图中的“阶段说明”入口。

点击后可以解释深睡、浅睡、REM、清醒分别表示什么。

说明应是纯解释性质，不需要在 ZeppBridge 内部进行健康诊断。

### 在床时长

当前代码直接显示：

“在床时长 未提供”。

同时前后端 `SleepSession` 模型都没有独立的在床时长字段。 

请检查 Zepp 原始响应中是否存在可靠的在床时长字段。

如果真实存在，应纳入 normalizer、storage、IPC 类型和 UI。

如果没有真实数据，继续显示“未提供”。

禁止根据睡眠阶段简单相加或自行推算出一个看似准确的值。

### 睡眠阶段

当前 `StageBar.vue` 根据深睡、浅睡、REM、清醒总分钟数直接拼接成比例条。

如果 Zepp 云端只提供阶段总时长，可以继续使用汇总比例形式。

如果实际原始响应存在可靠的逐阶段时间片，再考虑展示真正的睡眠时间轴。

禁止为了接近参考图而生成虚假的阶段顺序。

## 十、运动详情页面要求

视觉目标参考第五张图。

保留：

运动类型。

距离。

开始和结束时间。

时长。

消耗。

平均心率。

最高心率。

平均配速。

训练负荷。

VO₂max。

来源。

设备。

未获得轨迹时的说明。

主信息区和 4 × 2 指标矩阵需要尽量接近参考图的比例和视觉密度。

当前没有轨迹时显示说明是合理行为，不需要为了画面完整而生成假地图。

### 修复 GPS 和样本字段的前后端不一致

当前 TypeScript `Workout` 已经包含：

`gps_available`

`sample_count`

但 Rust 后端 `Workout` 模型没有对应字段。 

与此同时，运动详情页已经依赖这些字段判断如何描述轨迹状态。

请确认 Zepp 原始数据中实际能够获得什么，然后统一前后端模型。

如果能够获得 GPS 可用状态、样本数量或轨迹点，应准确传递。

如果不能获得，应删除或重新定义无法成立的前端判断。

## 十一、修正“同步时间”的数据语义

这是一个功能正确性问题。

当前睡眠详情使用 `session.end_time` 作为同步时间。

运动详情同样使用 `workout.end_time` 作为同步时间。 

这两个时间实际代表记录本身结束时间，不能称作云端同步时间。

当前 `SleepSession` 和 `Workout` 数据模型也没有记录级同步时间字段。 

请把语义修正。

如果数据库能够知道该记录写入或最近同步的真实时间，应增加类似 `synced_at` 或明确等价字段。

UI 中的“同步时间”必须展示真实同步时间。

如果无法获得真实记录级时间，可以显示全局最近云端同步时间，或者明确写“同步时间未提供”。

禁止使用运动结束时间或睡眠结束时间冒充同步时间。

## 十二、区分“数据来源”和“数据范围”

当前 `sourceLabel()` 将：

`user_fused` 显示为“Zepp 汇总”。

`device` 显示为“单设备”。

`mixed` 显示为“多来源”。

这里描述的是数据作用范围或融合范围。

参考图中的“数据来源 Zepp 云端”描述的是数据提供方。

这两种概念应当区分。

建议 UI 语义可以类似：

数据来源：Zepp 云端

数据范围：用户融合 / 单设备 / 多来源

具体是否同时展示两个字段可以根据页面空间决定。

不要把“单设备”直接当成数据来源。

## 十三、修复设备信息与具体记录不匹配的问题

当前睡眠和运动详情都会调用一个无参数的 `getDeviceProfile()`。

Rust 端的 `parse_device_profile()` 又直接从 Zepp 设备列表中取第一个设备。

这意味着详情页展示的设备信息未必属于当前这条睡眠或运动。

请使详情页设备信息与记录自身的 `device_id` 对应。

如果记录能够映射到具体设备：

显示该设备的名称。

显示该设备的固件。

显示对应设备 ID。

运动有序列号时展示正确序列号。

如果记录来自 Zepp 用户融合数据，无法唯一映射设备，则应明确显示融合来源或“设备未确定”。

禁止拿设备列表的第一台设备冒充当前记录设备。

## 十四、品牌 Logo 必须统一

当前项目同时存在两种 ZeppBridge 品牌图形。

`BrandMark.vue` 使用四根竖向圆角柱。

`src-tauri/icons/icon-source.svg` 使用一枚 Z 形的数据桥图形。

本次必须结束这种双品牌状态。

最终只允许存在一套核心 Brand Mark。

视觉方向以参考图左上角的 Z 形 ZeppBridge 图标为目标。

四根竖向柱状图标不再作为 ZeppBridge 主 Logo。

需要同时统一：

应用侧边栏 Logo。

窗口标题栏图标。

Windows 任务栏图标。

系统托盘图标。

桌面快捷方式。

开始菜单快捷方式。

安装包图标。

卸载程序图标。

未来 Release 使用的图标资产。

## 十五、修复桌面快捷方式旧图标问题

当前 Tauri bundle 使用多个独立资源：

32x32 PNG。

128x128 PNG。

256 级别资源。

ICO。

ICNS。

Windows Square Logo。

Tauri 配置也直接引用生成后的这些文件。

Windows 构建脚本目前只执行 Tauri build，没有看到在正式构建前强制根据 master icon 重新生成全部派生资源的步骤。

因此需要建立单一图标源。

最终应以一个 master SVG 或同等 master asset 为唯一真源。

所有 PNG、ICO、ICNS、Windows Store Logo 都应从它生成。

打包时必须保证这些资产属于同一个版本。

更换图标后进行一次干净构建。

同时测试全新安装和覆盖升级。

桌面快捷方式必须显示当前版本图标。

如果 Windows Explorer 图标缓存仍然显示旧图标，需要确认这是系统缓存问题，并保证新创建的快捷方式在缓存刷新后能读取到正确 ICO。

不要通过“托盘单独指定一张新图片，桌面继续使用旧 ICO”的方式绕过问题。

目前托盘使用的是 `default_window_icon()`，说明运行时本身已经具备共享同一应用图标的基础。

## 十六、重新统一普通界面 Icon

当前项目存在一个自定义的 `Icon.vue`，大量功能 Icon 都直接以 SVG path 手写在同一个组件内。

当前 package 中也没有统一的专业 Icon library。

这使不同 Icon 的造型、复杂度、视觉重量和细节很难保持一致。

本次可以继续使用自定义 Icon，也可以使用成熟 Icon set 作为基础，但最终必须达到一致。

要求：

所有导航 Icon 拥有一致视觉重量。

所有操作 Icon 拥有一致线宽。

端点和拐角语言统一。

图标尺寸形成明确规格。

不要在同一区域混用明显不同风格的线框图标和实心图标。

健康类别 Icon 可以使用少量实心或彩色强调，但必须形成固定规则。

品牌 Logo 与功能 Icon 必须彻底分开。

功能 Icon 不允许直接拿 Brand Mark 变形使用。

心率、睡眠、运动、步数、血氧、压力、HRV、训练负荷和 VO₂max 需要形成统一的健康指标 Icon 家族。

## 十七、浅色与深色模式

本轮主要参考深色模式。

同时不能破坏现有浅色模式和跟随系统功能。

深色模式完成后应检查：

主要文字。

次级文字。

边框。

卡片。

输入框。

Hover。

Focus。

Disabled。

危险状态。

成功状态。

浅色模式允许在视觉上稍作适配，但整体布局、Icon 几何和视觉层级必须一致。

## 十八、严禁为了“更像参考图”伪造健康数据

这是 ZeppBridge 的重要设计原则。

如果参考图中存在某个数据，而当前 Zepp 云端响应没有可靠提供：

显示“未提供”。

隐藏该项。

或者说明当前没有同步。

不要为了补齐画面而进行没有依据的数据推断。

不要推算不存在的 REM。

不要自行生成 GPS 轨迹。

不要根据其他时间冒充同步时间。

不要根据设备列表第一项冒充实际设备。

视觉完整度不能高于数据真实性。

## 十九、最终验收标准

完成后请使用我提供的前五张参考图进行并排比较。

重点检查：

相同或接近窗口尺寸下，内容占屏比例是否接近参考图。

概览页是否仍然显得缩在中间。

Settings、AI、Sleep、Workout 是否拥有统一的缩放感。

侧边栏宽度是否协调。

标题是否过大。

卡片 padding 是否过厚。

纵向空白是否过多。

主数字大小是否接近参考。

图标的线宽和视觉重量是否统一。

Brand Mark 是否完全统一。

新开详情页是否自动从顶部显示。

用户是否能够手动调整 UI 缩放。

桌面快捷方式、任务栏、窗口标题、托盘、安装程序是否显示同一版本图标。

同步时间是否拥有正确的数据语义。

详情页设备信息是否对应当前记录。

GPS 和样本字段前后端是否一致。

整个界面应明显接近参考图的专业完成度，同时保留 ZeppBridge 现有的数据真实性、本地优先和隐私原则。